




interface NeighborDiscovery{

    //need to ping nearest neighbor to find out who is around me

    //Add discover neighbors to known surroundings

    //TTL starts with 3 of neighbor according to Timer and reset TTL = 3 ticks in case there is a response
    
    //Remove node if TTL == 0
}